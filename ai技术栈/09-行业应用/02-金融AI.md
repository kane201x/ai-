# 金融 AI

## 1. 信用评分模型

### LightGBM 信用评分

```python
import lightgbm as lgb
import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.metrics import roc_auc_score, accuracy_score, classification_report

data = pd.read_csv("credit_data.csv")
features = ["age", "income", "loan_amount", "loan_term", "debt_ratio",
            "credit_history_len", "num_credit_cards", "utilization_rate",
            "num_late_payments", "num_inquiries", "employment_years"]
target = "default_flag"

X_train, X_test, y_train, y_test = train_test_split(
    data[features], data[target], test_size=0.2, random_state=42, stratify=data[target]
)

lgb_params = {
    "objective": "binary",
    "metric": "auc",
    "boosting_type": "gbdt",
    "num_leaves": 63,
    "learning_rate": 0.05,
    "feature_fraction": 0.8,
    "bagging_fraction": 0.8,
    "bagging_freq": 5,
    "reg_alpha": 1.0,
    "reg_lambda": 1.0,
    "min_child_samples": 20,
    "verbose": -1,
}

train_data = lgb.Dataset(X_train, label=y_train)
val_data = lgb.Dataset(X_test, label=y_test, reference=train_data)

model = lgb.train(
    lgb_params,
    train_data,
    num_boost_round=1000,
    valid_sets=[val_data],
    callbacks=[lgb.early_stopping(50), lgb.log_evaluation(100)]
)

y_pred = model.predict(X_test)
y_pred_binary = (y_pred >= 0.5).astype(int)

auc = roc_auc_score(y_test, y_pred)
print(f"AUC: {auc:.4f}")
print(classification_report(y_test, y_pred_binary))

importance = pd.DataFrame({
    "feature": features,
    "importance": model.feature_importance("gain")
}).sort_values("importance", ascending=False)
print(importance)
```

### 神经网络信用评分

```python
import torch
import torch.nn as nn
import numpy as np
from sklearn.preprocessing import StandardScaler

class CreditScoringNN(nn.Module):
    def __init__(self, input_dim):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(input_dim, 256),
            nn.BatchNorm1d(256),
            nn.ReLU(),
            nn.Dropout(0.3),
            nn.Linear(256, 128),
            nn.BatchNorm1d(128),
            nn.ReLU(),
            nn.Dropout(0.3),
            nn.Linear(128, 64),
            nn.ReLU(),
            nn.Linear(64, 1),
        )

    def forward(self, x):
        return torch.sigmoid(self.net(x)).squeeze()

scaler = StandardScaler()
X_train_scaled = scaler.fit_transform(X_train)
X_test_scaled = scaler.transform(X_test)

model_nn = CreditScoringNN(input_dim=X_train.shape[1])
criterion = nn.BCELoss()
optimizer = torch.optim.Adam(model_nn.parameters(), lr=1e-3, weight_decay=1e-4)

X_train_t = torch.FloatTensor(X_train_scaled)
y_train_t = torch.FloatTensor(y_train.values)
X_test_t = torch.FloatTensor(X_test_scaled)
y_test_t = torch.FloatTensor(y_test.values)

for epoch in range(200):
    model_nn.train()
    optimizer.zero_grad()
    outputs = model_nn(X_train_t)
    loss = criterion(outputs, y_train_t)
    loss.backward()
    optimizer.step()

    if epoch % 20 == 19:
        model_nn.eval()
        with torch.no_grad():
            val_preds = model_nn(X_test_t)
            val_auc = roc_auc_score(y_test_t.numpy(), val_preds.numpy())
        print(f"Epoch {epoch}: Loss={loss.item():.4f}, Val AUC={val_auc:.4f}")
```

### 欺诈检测 (XGBoost)

```python
import xgboost as xgb
import pandas as pd
import numpy as np
from sklearn.model_selection import StratifiedKFold
from sklearn.metrics import precision_recall_curve, average_precision_score

txn_data = pd.read_parquet("transactions.parquet")
txn_features = ["amount", "txn_hour", "txn_dow", "days_since_last_txn",
                "avg_amount_30d", "txn_count_30d", "merchant_category",
                "distance_from_home", "device_risk_score", "ip_country_match"]

skf = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)
fold_scores = []

for fold, (train_idx, val_idx) in enumerate(skf.split(txn_data[txn_features], txn_data["is_fraud"])):
    X_tr, X_val = txn_data.iloc[train_idx][txn_features], txn_data.iloc[val_idx][txn_features]
    y_tr, y_val = txn_data.iloc[train_idx]["is_fraud"], txn_data.iloc[val_idx]["is_fraud"]

    dtrain = xgb.DMatrix(X_tr, label=y_tr)
    dval = xgb.DMatrix(X_val, label=y_val)

    params = {
        "objective": "binary:logistic",
        "eval_metric": "aucpr",
        "max_depth": 8,
        "learning_rate": 0.1,
        "subsample": 0.8,
        "colsample_bytree": 0.8,
        "scale_pos_weight": sum(y_tr == 0) / sum(y_tr == 1),
        "min_child_weight": 5,
        "gamma": 0.1,
        "seed": 42,
    }

    model_xgb = xgb.train(
        params, dtrain, num_boost_round=500,
        evals=[(dval, "val")],
        early_stopping_rounds=30,
        verbose_eval=False
    )

    y_pred_xgb = model_xgb.predict(dval)
    ap = average_precision_score(y_val, y_pred_xgb)
    fold_scores.append(ap)
    print(f"Fold {fold}: AP={ap:.4f}")

print(f"Avg AP: {np.mean(fold_scores):.4f} +/- {np.std(fold_scores):.4f}")
```

### 量化因子计算与回测

```python
import pandas as pd
import numpy as np
import talib

price_data = pd.read_csv("stock_daily.csv", parse_dates=["date"], index_col="date")

def compute_alpha_factors(df):
    factors = pd.DataFrame(index=df.index)
    factors["ma5"] = df["close"].rolling(5).mean() / df["close"]
    factors["ma20"] = df["close"].rolling(20).mean() / df["close"]
    factors["ma60"] = df["close"].rolling(60).mean() / df["close"]
    factors["volume_ratio"] = df["volume"] / df["volume"].rolling(20).mean()
    factors["volatility"] = df["returns"].rolling(20).std()
    factors["rsi"] = talib.RSI(df["close"].values, timeperiod=14)
    factors["macd"] = talib.MACD(df["close"].values)[0] / df["close"]
    factors["bb_position"] = (df["close"] - talib.BBANDS(df["close"].values)[2]) / (
        talib.BBANDS(df["close"].values)[0] - talib.BBANDS(df["close"].values)[2]
    )
    factors["obv"] = talib.OBV(df["close"].values, df["volume"].values)
    factors["obv_ma_ratio"] = factors["obv"] / factors["obv"].rolling(20).mean()
    factors["momentum_1m"] = df["close"].pct_change(21)
    factors["momentum_3m"] = df["close"].pct_change(63)
    factors["turnover"] = df["volume"] * df["close"] / 1e8
    factors["price_to_ma20"] = df["close"] / df["close"].rolling(20).mean()
    return factors.fillna(0)

def simple_factor_backtest(factors, forward_returns, factor_name):
    ranked = factors[factor_name].rank(pct=True)
    long = ranked >= 0.8
    short = ranked <= 0.2
    long_ret = forward_returns[long].mean(axis=1)
    short_ret = forward_returns[short].mean(axis=1)
    pnl = long_ret - short_ret
    sharpe = np.sqrt(252) * pnl.mean() / pnl.std()
    return {"factor": factor_name, "sharpe": sharpe, "avg_return": pnl.mean(), "win_rate": (pnl > 0).mean()}

df["returns"] = df["close"].pct_change()
forward_5d = df["returns"].shift(-5).rolling(5).sum()
all_factors = compute_alpha_factors(df)
results = [simple_factor_backtest(all_factors, forward_5d, col) for col in all_factors.columns]
results_df = pd.DataFrame(results).sort_values("sharpe", ascending=False)
print(results_df)
```

### 信用评分模型对比

| 模型 | AUC | Precision@5% | 训练时间 | 可解释性 | 适用场景 |
|------|-----|-------------|----------|----------|----------|
| Logistic Regression | 0.72 | 0.35 | 10s | 高 | 合规要求高 |
| Random Forest | 0.83 | 0.52 | 120s | 中 | 通用评分 |
| LightGBM | 0.87 | 0.58 | 45s | 中 | 大规模数据 |
| TabNet | 0.86 | 0.56 | 300s | 低 | 非结构化特征 |

### 欺诈检测方法对比

| 方法 | Recall | FPR | 实时性 | 特点 |
|------|--------|-----|--------|------|
| 规则引擎 | 0.60 | 0.5% | 1ms | 可解释强，覆盖有限 |
| Isolation Forest | 0.72 | 1.2% | 5ms | 无监督异常检测 |
| XGBoost | 0.88 | 0.8% | 3ms | 梯度提升最佳实践 |
| GNN (GraphSAGE) | 0.91 | 0.6% | 15ms | 关系图反欺诈 |
| AutoEncoder | 0.76 | 1.5% | 2ms | 无监督重建误差 |

### 量化因子类型对比

| 因子类型 | 举例 | IC均值 | 换手率 | 容量 |
|----------|------|--------|--------|------|
| 动量因子 | 过去1月收益率 | 0.04 | 高 | 大 |
| 反转因子 | 过去5日跌幅 | -0.03 | 高 | 中 |
| 质量因子 | ROE / 毛利率 | 0.05 | 低 | 大 |
| 低波因子 | 过去60日波动率 | 0.03 | 低 | 大 |
| 机器学习合成因子 | AutoAlpha 组合 | 0.08 | 中 | 中 |

### 欺诈检测流程

```mermaid
graph TD
    A[实时交易请求] --> B{规则引擎}
    B -->|通过| C[特征提取]
    B -->|拒绝| D[人工审核]
    C --> E[设备指纹]
    C --> F[行为序列]
    C --> G[关系图特征]
    E --> H[XGBoost模型]
    F --> H
    G --> I[GNN模型]
    H --> J{风险评分}
    I --> J
    J -->|低风险| K[自动放行]
    J -->|中风险| L[二次验证]
    J -->|高风险| M[实时拦截]
    L --> N[OTP/生物识别]
    N -->|通过| K
    N -->|失败| M
    M --> O[加入黑名单]
    O --> P[关联账户冻结]
```

### 量化交易流程

```mermaid
graph LR
    A[行情数据] --> B[因子计算]
    B --> C[IC分析]
    C --> D[因子筛选]
    D --> E[模型训练]
    E --> F[信号生成]
    F --> G[风险控制]
    G --> H[订单执行]
    H --> I[成交回报]
    I --> J[绩效分析]
    J --> B
    J --> K[归因分析]
    K --> L[因子迭代]
    L --> D
```

### 量化交易策略评估指标

| 指标 | 定义 | 优秀标准 |
|------|------|---------|
| Sharpe Ratio | (Rp-Rf)/σp | >2.0 |
| Max Drawdown | 最大回撤 | <15% |
| Calmar Ratio | 年化收益/最大回撤 | >3.0 |
| Win Rate | 交易胜率 | >55% |
| Profit Factor | 总盈利/总亏损 | >2.0 |
| Information Ratio | 超额收益/跟踪误差 | >1.5 |

## 2. 大模型在金融

### 智能投研 Agent

```mermaid
graph TD
    A[用户指令] --> B[Agent 规划器]
    B --> C[信息检索]
    B --> D[数据分析]
    B --> E[报告生成]
    C --> F[新闻爬取]
    C --> G[财报解析 RAG]
    C --> H[行业数据库]
    D --> I[财务指标计算]
    D --> J[估值模型]
    D --> K[同业对比]
    F --> L[综合分析]
    G --> L
    H --> L
    I --> L
    J --> L
    K --> L
    L --> M[投资建议]
    M --> N{用户反馈}
    N -->|追问| B
    N -->|采纳| O[输出报告]
```

```python
import json
from openai import OpenAI

client = OpenAI()

def research_agent(query: str) -> dict:
    """智能投研 Agent：自主完成信息收集、分析、报告"""
    messages = [
        {"role": "system", "content": """你是一位资深行业研究员。请按以下步骤分析：
1. 检索该公司的核心业务与财务数据
2. 分析行业竞争格局与上下游
3. 评估估值水平与成长性
4. 给出投资建议与风险提示
输出为 JSON 格式。"""},
        {"role": "user", "content": query}
    ]
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=messages,
        response_format={"type": "json_object"},
        temperature=0.1
    )
    return json.loads(response.choices[0].message.content)

# 使用示例
result = research_agent("分析宁德时代2026年Q1财报，对比比亚迪和国轩高科")
print(f"核心结论: {result['conclusion']}")
print(f"估值判断: {result['valuation']}")
```

### 财报智能分析 RAG

```mermaid
graph LR
    A[PDF 财报] --> B[文档解析]
    B --> C[表格提取]
    B --> D[文本分块]
    C --> E[向量化]
    D --> E
    E --> F[向量数据库]
    G[用户问题] --> H[语义检索]
    F --> H
    H --> I[LLM 生成回答]
    I --> J[附引用来源]
```

```python
from langchain_community.document_loaders import PyPDFLoader
from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain_openai import OpenAIEmbeddings, ChatOpenAI
from langchain_community.vectorstores import FAISS

def build_financial_rag(pdf_path: str):
    loader = PyPDFLoader(pdf_path)
    docs = loader.load()
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=1000, chunk_overlap=200,
        separators=["\n\n", "\n", "。", "；", " "]
    )
    chunks = splitter.split_documents(docs)
    vectorstore = FAISS.from_documents(chunks, OpenAIEmbeddings())
    return vectorstore

def query_financial_report(vectorstore, question: str) -> str:
    retriever = vectorstore.as_retriever(search_kwargs={"k": 5})
    llm = ChatOpenAI(model="gpt-4o", temperature=0)
    from langchain.chains import RetrievalQA
    qa = RetrievalQA.from_chain_type(llm=llm, retriever=retriever)
    return qa.invoke(question)

vs = build_financial_rag("2025_annual_report.pdf")
answer = query_financial_report(vs, "公司2025年毛利率变化趋势及原因")
print(answer)
```

### 智能风控合规

```python
def compliance_check(contract_text: str, regulations: list) -> dict:
    """用 LLM 检查合同条款的合规性"""
    prompt = f"""作为金融合规专家，检查以下合同条款是否违反监管规定。

需遵循的法规：{', '.join(regulations)}

合同内容：
{contract_text}

请按以下格式返回：
{{
    "clause": "问题条款摘要",
    "risk_level": "high/medium/low",
    "violated_regulation": "违反的具体法规",
    "suggestion": "修改建议"
}}"""
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[{"role": "user", "content": prompt}],
        response_format={"type": "json_object"},
        temperature=0.0
    )
    return json.loads(response.choices[0].message.content)

def monitor_regulatory_changes(new_regulation: str, existing_policies: list) -> list:
    """法规变化自动影响评估"""
    prompt = f"""分析以下新法规对公司现有政策的潜在影响。

新法规：
{new_regulation}

现有政策：
{chr(10).join(f'- {p}' for p in existing_policies)}

输出 JSON 数组，每个元素包含 policy、impact_level、action_required。"""
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[{"role": "user", "content": prompt}],
        response_format={"type": "json_object"},
        temperature=0.0
    )
    return json.loads(response.choices[0].message.content)
```

### 智能客服与财富管理

| 场景 | 传统方案 | LLM 方案 | 优势 |
|------|---------|----------|------|
| 客户咨询 | 关键词匹配 + 人工 | 意图识别 + RAG 知识库 | 理解复杂语义，24h 在线 |
| 投顾建议 | 模板化资产配置 | Agent 个性化分析 | 千人千面，实时调整 |
| 投诉处理 | 工单系统流转 | 情感分析 + 自动回复 | 即时响应 >80% |
| 产品对比 | 人工查询表格 | 结构化提取 + 对比生成 | 3s 输出多维对比 |
| 风险预警 | 规则引擎触发 | 事件驱动 + 影响评估 | 提前识别潜在风险 |

```python
def financial_advisor(profile: dict, market_data: dict) -> dict:
    """个性化财富管理建议 Agent"""
    prompt = f"""作为持牌投资顾问，为以下客户提供资产配置建议。

客户画像：
{json.dumps(profile, ensure_ascii=False)}

当前市场环境：
{json.dumps(market_data, ensure_ascii=False)}

输出 JSON，包含：asset_allocation（各类资产比例）、
recommended_products（推荐产品列表）、risk_warnings（风险提示）。"""
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[{"role": "user", "content": prompt}],
        response_format={"type": "json_object"},
        temperature=0.2
    )
    return json.loads(response.choices[0].message.content)
```

### 舆情分析与宏观预测

```python
def sentiment_analysis(news_list: list[str]) -> dict:
    """金融新闻舆情分析"""
    prompt = f"""分析以下金融新闻的整体市场情绪和关键主题。

新闻列表：
{chr(10).join(f'- {n}' for n in news_list)}

输出 JSON：
{{
    "overall_sentiment": "positive/negative/neutral",
    "sentiment_score": 0.0-1.0,
    "key_topics": ["主题1", "主题2"],
    "impact_analysis": "对市场/行业的潜在影响",
    "related_tickers": ["相关股票代码"]
}}"""
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[{"role": "user", "content": prompt}],
        response_format={"type": "json_object"},
        temperature=0.1
    )
    return json.loads(response.choices[0].message.content)
```

### 保险核保与理赔

```python
def underwriting_assessment(applicant_info: dict, medical_records: str) -> dict:
    """AI 核保评估"""
    prompt = f"""作为保险核保师，评估以下投保申请。

申请人信息：{json.dumps(applicant_info, ensure_ascii=False)}
健康告知/体检报告：{medical_records}

评估：
1. 风险等级（优选/标准/次标准/拒保）
2. 加费比例（如适用）
3. 除外责任建议
4. 需要进一步核保的问题

输出 JSON 格式。"""
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[{"role": "user", "content": prompt}],
        response_format={"type": "json_object"},
        temperature=0.1
    )
    return json.loads(response.choices[0].message.content)

def claim_processing(claim_docs: str, policy_terms: str) -> dict:
    """AI 理赔审核"""
    prompt = f"""作为理赔专员，审核以下理赔申请。

理赔材料：{claim_docs}
保单条款：{policy_terms}

输出 JSON：
{{
    "claim_decision": "approve/reject/partial",
    "approved_amount": 金额,
    "reject_reason": "拒赔理由（如适用）",
    "pending_review": "需人工复核的项目",
    "processing_time": "预估处理时间"
}}"""
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[{"role": "user", "content": prompt}],
        response_format={"type": "json_object"},
        temperature=0.0
    )
    return json.loads(response.choices[0].message.content)
```

### 大模型金融应用全景

```mermaid
graph TD
    subgraph 数据层
    A1[行情数据] --> B
    A2[财报/公告] --> B
    A3[新闻舆情] --> B
    A4[社交数据] --> B
    A5[监管法规] --> B
    end
    subgraph 模型层
    B[数据预处理] --> C[金融 LLM]
    C --> D[BloombergGPT]
    C --> E[FinBERT]
    C --> F[通用 LLM]
    end
    subgraph 应用层
    C --> G[智能投研]
    C --> H[风控合规]
    C --> I[财富管理]
    C --> J[量化交易]
    C --> K[客服运营]
    C --> L[保险科技]
    end
    subgraph 保障层
    M[可解释性 SHAP/LIME] --> C
    N[数据隐私 差分隐私] --> B
    O[人工复核] --> C
    end
```

## 3. 2025-2026 趋势
- **Agentic 投研**：自主信息收集-分析-交易闭环，多 Agent 协作决策
- **AI 合规助手**：法规变化自动追踪与影响评估，降低合规成本 60%+
- **合成金融数据**：差分隐私生成替代真实数据，解决数据稀缺与隐私问题
- **可解释 AI 评分**：SHAP+LIME 满足监管可解释性要求
- **多模态金融分析**：财报图表+新闻+价格序列联合建模，提升预测准确率
- **推理时扩展**：O1/DeepSeek-R1 类模型在复杂金融推理任务中的应用
- **端到端信用评估**：结合 LLM 文本理解与传统表格数据的融合评分模型
